import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'coding-challenge-angular',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
